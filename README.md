# Get-This-Done

A to-do list application with a customizable background. Pick a bundled scene or paste an image URL from the web. Tasks and the chosen background are saved in the browser.

```bash
npm install
npm run dev
```
